#include <bits/stdc++.h>
using namespace std;

int main() {
    int N, W; cin >> N >> W;
    //cin >> ;
    vector<int> w(N, 0);
    vector<int> v(N, 0);

    for(int i = 0; i < N; ++i){
        cin >> w.at(i) >> v.at(i);
    }

    for(int i = 0; i < N; ++i){
        for(int j = 0; j < W; ++j){
            
        }
    }

    //cout << << endl;

    return 0;
}