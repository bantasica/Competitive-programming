#include <bits/stdc++.h>
using namespace std;

int main() {
    string S;
    string A, B;

    cin >> S;
    A = S[0] + S[1];
    B = S[2] + S[3];

    string res;

    //if(stoi(A) > 1 && )

    //cout << << endl;

    return 0;
}